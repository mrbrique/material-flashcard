import { ParseIntPipePipe } from './parse-int-pipe.pipe';

describe('ParseIntPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseIntPipePipe();
    expect(pipe).toBeTruthy();
  });
});
